Welcome back. Lesson 01 gave us the smallest legal collector: a receiver wired straight into the debug exporter, no processors at all. Today we complete the picture: the full receiver -> processors -> exporters pipeline, still on the pull/scrape model everyone knows. Two things to internalize today: processors are ordered ON PURPOSE, and exporters can fan out. By the end, metrics will land in the LGTM stack without Prometheus scraping anything.