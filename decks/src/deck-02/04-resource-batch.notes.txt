resource/env: 'upsert' means insert-or-overwrite. The powerful idea for our migration: environment tagging becomes platform configuration instead of app configuration. Today every service carries its env in its own config; after the shift, the collector near the workload stamps it. batch: production would use a shorter timeout or send_batch_size; we picked 2 seconds so the audience can SEE the rhythm in docker compose logs -f otelcol - the debug dumps arrive in 2-second heartbeats instead of per-scrape. Demo beat: restart otelcol, watch the log cadence change from lesson 01.