Do this one live: open localhost:9090, Status -> Targets, let the empty page sink in, then run a query that returns data. This inversion is the conceptual pivot of the whole course: the backend stops reaching into workloads, and a pipeline component near the workload takes over delivery. For the devops folks: this is what decouples app topology from monitoring config - no more scrape_config sprawl, no more service discovery gymnastics per team. The pull model didn't die; it got centralized into a component we control.