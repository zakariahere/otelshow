Close on the operational framing: nobody rewrote a service today. We flipped env vars, and the app now emits traces, metrics and logs over one protocol to one address it doesn't even resolve beyond the compose network. Tee up session 04: everything we just watched flow through the debug exporter — including player=alice sitting in a span attribute — is about to get filtered, redacted and enriched in flight, again without touching the app.