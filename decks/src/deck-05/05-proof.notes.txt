All three snippets are real output from verifying this branch - not mockups. Tempo's search API returns our /play traces (traceIDs shortened on the slide); Loki returns 3 streams for the service selector; Prometheus has the http_server_request_duration histogram that can ONLY have arrived via OTLP push (nothing scrapes it). Point at the marked line: /roll sleeps 1.2s when you roll a 6, and that outlier is already visible in the raw search response before we even open Grafana. Next slide: the guided tour.