Real config from the branch, trimmed for the slide (full file: otel/otel-collector.yaml on 06-advanced). Point out the naming: 0.158 renamed spanmetrics to span_metrics — old names still work but log deprecation warnings. And the wiring rule at the bottom: a connector appears as an EXPORTER in one pipeline and a RECEIVER in another; that's the whole trick. Mention the validate command: docker run --rm -v <cfg>:/cfg otel/opentelemetry-collector-contrib:0.158.0 validate --config /cfg.