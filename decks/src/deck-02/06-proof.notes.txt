This output is real - captured from the running lab, not mocked. Run the same curl live, or open Grafana Explore and query rate(dice_rolls_total[1m]). Pause on the otel_scope_name label: Prometheus received this series with OTel scope metadata attached, which a normal scrape would never produce. It's forensic proof of the delivery path: app exposed -> collector scraped -> collector remote-wrote. Then roll the dice a few times (curl /roll) and re-run the query to show the counter moving through the whole pipe.