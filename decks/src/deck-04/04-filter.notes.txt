The story: on branch 03 the debug log was full of GET /actuator/prometheus spans. Where from? OUR OWN prometheus receiver scraping the app every 5s - the agent traces those requests like any other. One OTTL condition kills them. Real measurement from this session: 7 actuator spans in the window before, 0 after the restart. error_mode ignore means a bad condition drops nothing - the record passes and the error is logged.
