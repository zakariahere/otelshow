Until now exporters sent data OUT of the collector. A connector is an exporter whose destination is another pipeline inside the same collector. span_metrics is the killer example: the RED dashboard your SRE team wants, computed from spans you already have. forward looks pointless until you need to run different processors before and after a branch point — which is precisely the next slide.