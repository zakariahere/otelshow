Show the branch diff live if you can — git diff 02-prom-full-pipeline..03-otlp-receiver. This env block is the complete application-side migration. Walk the vars top to bottom: JAVA_TOOL_OPTIONS wakes the agent that was baked into the image on day one; SERVICE_NAME becomes service.name on every span, metric and log; ENDPOINT+PROTOCOL point at the collector over gRPC; RESOURCE_ATTRIBUTES stamps environment and team; the 10s metric interval is just for a snappy demo (default is 60s). Then the trap that cost us a debugging session: agent 2.x does NOT bridge Micrometer by default. Your @Timed, your Counters — silently absent from OTLP until you flip that flag.