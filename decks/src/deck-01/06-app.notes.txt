Worth pausing on, because it's the argument for starting the migration here. Lesson 3 will change the app's runtime (env vars, an agent). This lesson changes nothing at all — the collector is just another client of an endpoint we already serve. So rolling out collectors is operationally independent of touching applications: infra can go first, apps follow at their own pace. That decoupling is what makes the whole shift incremental instead of big-bang.