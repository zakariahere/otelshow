This is us, today. Traces go to a Kafka topic with publisher code we wrote and maintain. Logs go to another topic, in another format. Metrics don't go anywhere — Prometheus comes and gets them. None of these three pipelines knows about the others, which is why correlating a trace with its logs is a manual sport. And every single app carries the addresses of every backend in its config.