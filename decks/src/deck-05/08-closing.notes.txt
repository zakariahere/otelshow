Land the org-level claim: the collector is the abstraction boundary between application teams and observability infrastructure. Devs: instrument once (or let the agent do it), point at one address. DevOps: own routing, credentials, retention and vendor choice in one YAML file. Migrating from vendor A to vendor B - or running both during evaluation, because an exporter list is fan-out - never touches an application. Tease lesson 06: today data flowed THROUGH the collector; next time the collector starts making decisions (tail sampling) and minting new data (span metrics).