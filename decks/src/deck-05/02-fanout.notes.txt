Walk the table row by row. Point out that the pipelines were already split per signal in lesson 3 - all we do today is swap 'debug' for a real destination on each, keeping debug alongside (an exporter list IS fan-out: same data, multiple destinations). Two housekeeping notes: the component rename (otlphttp -> otlp_http, old name still works but warns), and debug demoted from detailed to normal - we still see batch counts in the log, but we stop drowning in datapoint dumps. The old scrape pipeline still ships via remote write: coexistence continues.