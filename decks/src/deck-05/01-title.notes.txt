Welcome back. Lessons 1-4 built the pipeline: receivers got data in, processors cleaned it up in flight. But so far (almost) everything ended in the debug exporter - a log file. Today the data finally lands somewhere useful: Tempo for traces, Loki for logs, Prometheus for metrics. The headline to keep in mind the whole session: the APP still only knows one address - the collector. Every backend decision we make today is invisible to the application team.