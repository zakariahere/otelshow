The whole file, unabridged — I want the room to see it fits on a slide. The block to linger on is config:. This isn't an imitation of Prometheus config, it's the real parser and the real scrape manager compiled into the collector. Whatever scrape_configs we run in production Prometheus today would paste in here. So the pull world isn't being deprecated by this migration — it's being absorbed. And note the pipeline: metrics, from prometheus, to debug. Nothing else. That's a complete, legal, running collector.