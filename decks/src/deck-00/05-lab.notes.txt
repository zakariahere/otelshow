Our lab. A real Spring Boot 4.1 app on Java 25 — it rolls dice, calls itself over HTTP, logs, counts rolls, and has an endpoint that deliberately explodes. The observability backends come from Grafana's all-in-one LGTM image so we don't spend the course on infrastructure. And one detail I planted deliberately: the OpenTelemetry Java agent jar is already inside the app image, doing nothing. Keep that in mind for session three — it's the punchline of the whole migration story.