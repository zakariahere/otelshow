Real output from the repo, main branch. The app works — alice played, rolled a six, and by design a six makes the request sleep 1.2 seconds. Below it, the entirety of what Prometheus can learn: a counter went up by one. That slow request produced a trace and log lines, but in the current world those live in topics with custom plumbing per app. Notice what's missing: nothing here connects the slow request to its logs or its trace. That's the gap.