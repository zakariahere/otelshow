Same session output, and the marked line is the whole slide: this ordinary log.info call from DiceController left the app already carrying the trace ID and span ID of the request that produced it. Compare with what we do today: shipping logs to a topic, then trying to join them to traces by timestamp and pod name in the backend. Here the join key travels WITH the record because the agent instruments the logging framework itself. When we reach Grafana in lesson 5, this is what powers the log-to-trace jump button.