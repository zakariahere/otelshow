The humbling slide. After both redaction statements we grepped the debug log for player=alice: 4 hits. Every one inside a metric EXEMPLAR - histograms keep original span attributes, and our transform runs in the traces pipeline only. Plus 'game finished for alice' sails past the regex. Discussion for the team: where else would you have to repeat this? (Answer: every pipeline, every signal, every new attribute someone adds next quarter.) The durable fix is upstream - don't record it.
