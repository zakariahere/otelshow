Run the git diff live before showing this slide. Point out what did NOT change: no receiver change, no app change. The whole lesson lives in the processors block plus one exporter. Walk through the three processors top to bottom and stress that the order they appear in the pipeline definition is the order data flows through them. memory_limiter first, batch last - we'll spend a slide on each rule.