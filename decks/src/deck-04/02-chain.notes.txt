Walk the flow left to right. memory_limiter protects the collector itself - it refuses data under pressure, which is why it must be FIRST; anything before it is unprotected. batch is LAST so every processor upstream works on single items while the network sees chunks. The middle is ours: detect, stamp, tag, drop, mutate. Emphasize: this order is a choice we will defend on slide 7 (the exercise).
