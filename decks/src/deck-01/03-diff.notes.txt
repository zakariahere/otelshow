This is how the whole course works: switch branch, diff against the previous one, and the diff IS the lesson. Today's diff: the otelcol service — pinned image, read-only config mount, and the two OTLP ports. Note we publish 4317/4318 already even though nothing pushes yet: the collector owns those ports from day one, so when lesson 3 arrives the only thing that changes is who talks to them. One deliberate detail: the LGTM box does NOT publish 4317 — telemetry has to go through our collector.